# 5. Startup Failure Matrix

Each scenario traced through actual code (no device access in this sandbox — this is a code-path simulation, not an executed test).

| # | Scenario | Behavior | Graceful? |
|---|---|---|---|
| 1 | Missing environment variables | `validateConfig()` returns `{ok:false, missing:[...]}`; `RootLayout` renders `ConfigErrorScreen` instead of the provider tree; `trackStartupEvent("startup_failure", {reason:"missing_config"})` fires; `RootLayout`'s own effect calls `SplashScreen.hideAsync()` directly on this path (fixed during this audit — see Technical Debt TD-2) | ✅ Yes |
| 2 | Repository initialization failure | `createAppContainer()` throws inside `DependencyProvider`'s `useMemo` | ✅ Yes — caught by the `ErrorBoundary` that wraps `DependencyProvider` in the tree, renders `ErrorFallback` (visible "Something went wrong" screen with a restart button), and reports via `reportError` → Sentry if configured |
| 3 | SecureStore unavailable | `core/storage/secureStore.ts`'s `secureGet`/`secureSet`/`secureDelete` each wrap their native calls in try/catch, returning `null`/no-op on failure; `cryptoBox.ts`'s `getOrCreateKey` treats a null read as "no key yet" and generates + stores a fresh one (also swallowed if the subsequent `secureSet` fails) | ✅ Yes — worst case, a session that can't be encrypted/decrypted is treated as "no session," which is the same safe fallback used everywhere persisted state can't be trusted |
| 4 | Auth restore timeout | `AuthContext.tsx`'s `AUTH_STATE_TIMEOUT_MS` (6000ms) forces `loading=false` regardless of whether `onAuthStateChanged` ever fires; `trackStartupEvent("auth_restore_complete")` fires either way | ✅ Yes — `Gate` is never blocked past 6 seconds by auth alone |
| 5 | Background module (TaskManager) unavailable | `getTaskManager()` returns `null`; `defineTask` registration is skipped; `startBackgroundLocationTracking()` early-returns `false` | ✅ Yes — this is the fix verified in this pass. Does not affect app **startup** at all (background location is only touched when an SOS is actually triggered), and no longer affects app **launch** either (was the root cause of the original white-screen bug, now fixed) |
| 6 | TaskManager unavailable (duplicate of #5, listed separately per the brief) | Same as #5 | ✅ Yes |
| 7 | Notification module unavailable | Not explicitly guarded — `expo-notifications` is imported unconditionally and used at both module scope (`enableNotificationHandler()`) and inside `Gate`. No code path currently simulates this returning `null`/throwing at the JS layer, since Expo's typed API doesn't expose a "module missing" return value the way a capability-checked module would | ⚠️ Unverified — see Initialization Risk Assessment's residual finding and Technical Debt TD-1. Mitigated at the "would be silent" layer by `installCrashBeforeRenderHandler()`, which would at least emit a `crash_before_render` telemetry event if this ever threw during the startup window |
| 8 | Slow startup (network/disk latency) | Bounded by the 6-second auth timeout (largest single contributor) and the 4-second font-load timeout; `getElapsedSinceStart()` is now recorded on every `startup_complete`/`auth_restore_complete`/`navigation_ready` event for real visibility into how slow "slow" actually is in production | ✅ Yes — every slow path has an explicit ceiling; nothing is unbounded |
| 9 | Network unavailable | `initFirebase`/`initSupabase` only construct client objects — no network call happens at module load. `onAuthStateChanged` on a fully offline device with no cached session will not fire; the 6-second timeout still applies, so the app proceeds (likely to an anonymous-sign-in attempt per `AuthContext`'s existing fallback effect, which itself fails silently offline and is logged via `logger.warn`, not thrown) | ✅ Yes — no startup path assumes network reachability as a precondition for reaching the home screen |

## Simulated vs. executed

Every row above was verified by reading the actual guarding code (try/catch boundaries, timeouts, `ErrorBoundary` placement) — none of it was executed against a real device, a killed network connection, or an actual missing native module. Scenarios 1–4, 8, 9 were already true before this pass (pre-existing, previously-reviewed mitigations from earlier hardening work); scenarios 5–6 are the fix delivered in this pass; scenario 7 is the one open, unverified item.
